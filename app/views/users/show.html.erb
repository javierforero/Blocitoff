<div class="container">
  <div class="row">
    <h1>Welcome! <%= @user.name %></h1>
      <div class="mid-col-4"></div>
      <div class="mid-col-4"><%= image_tag @user.avatar_url(250)%></div>
      <div class="mid-col-4"></div>
    <small>email: <%= @user.email%>,</small>
    <small>count of items: <%= @user.items.count%></small>
    <h2>To-do Items </h2>
    <%= render "items/items", items: @user.items %>
    <%= render "items/form", item: Item.new, user: @user %>
 </div>
</div>
